#function/matthod = การทำงานของ จะไม่ทำงานถ้าเราไม่เรียกใช้ call function
#function =standard function/built-in function มากับภาษาโปรแกรม โค้ดสั้น
#user-defined function = ฟังก์ชันที่dafสร้างขึ้นมาเอง  เป็นสัดส่วน
#third party function = ฟังก์ชันที่คนอื่นสร้างไว้ คนอื่น   โคค้ดสั้น
#function = มี 4 ประเภท 

# no parameter no return
#have parameter no return
#no parameter have return 
#have parameter have return 
